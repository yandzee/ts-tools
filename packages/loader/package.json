{
  "name": "@repo/loader",
  "private": false,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "tsdown --config tsdown.dev.ts",
    "build": "tsdown --config tsdown.build.ts",
    "check:types": "tsc --noEmit",
    "check:lint": "biome check",
    "check:test": "vitest"
  },
  "devDependencies": {
    "tsdown": "^0.16.4",
    "typescript": "~5.9.3",
    "vitest": "^4.0.8"
  },
  "dependencies": {
    "@repo/common": "workspace:^",
    "@repo/tsconfig": "workspace:^",
    "@repo/tsdown": "workspace:^",
    "@types/node": "^24.10.1",
    "es-toolkit": "^1.41.0"
  }
}
